# Reproducible environments

Container or environment lock files belong here once the first retrieval, ML or
simulation workflow is approved. Do not encode credentials, licensed software blobs or
cluster-specific secrets in an image definition.
