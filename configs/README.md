# Configuration

Track non-secret defaults and controlled vocabularies here. Put machine paths,
credentials, database passwords and cluster account details in ignored local files
or environment variables, never in Git.
