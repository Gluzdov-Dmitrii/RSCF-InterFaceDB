# Model artifacts

Serialized models, checkpoints and prediction arrays are not stored in Git. Track model code, parameters, metrics and small test fixtures. Use DVC after the project remote is approved.
