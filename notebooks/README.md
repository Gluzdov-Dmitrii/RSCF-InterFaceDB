# Notebooks

Notebooks are for exploration, not the authoritative pipeline. Move reusable logic to
`src/interface_db`, clear bulky outputs before committing and record the input dataset
hash in the notebook header.
