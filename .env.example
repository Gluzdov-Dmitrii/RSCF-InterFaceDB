# Optional contact used by polite scholarly APIs. Do not commit a personal .env.
CROSSREF_MAILTO=research@example.org

# Configure only after a durable institutional or cluster storage location is chosen.
DVC_REMOTE_URL=
