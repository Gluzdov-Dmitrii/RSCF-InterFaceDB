# Simulations

Version calculation templates, convergence protocols and small test inputs. Place run
directories, trajectories, checkpoints and wavefunctions under `simulations/runs/` or
cluster storage. Every run must reference a hypothesis, an input hash, a code version,
a convergence criterion and a stop rule.
