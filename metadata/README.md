# Release metadata

This directory contains controlled vocabularies and will hold lightweight DataCite and RO-Crate templates for public database releases. `vocabularies/property_terms.csv` is the authoritative v0.2 list of 43 property terms and must stay semantically synchronized with the JSON and SQL schemas. Generate release metadata only from a validated curated snapshot with a documented license and stable version.
